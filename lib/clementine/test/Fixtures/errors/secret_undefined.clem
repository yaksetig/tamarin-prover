// Should fail with E0205: claim secret references undefined term.

protocol SecretUndefined {
  builtins { hashing }

  principal A {
    generates skA
  }

  step m1 : A local {
    new x
    claim secret(undefined_var)        // <-- undefined_var is not bound
  }

  verify {
    executable
  }
}
